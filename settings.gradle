pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        mavenLocal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
        id 'io.github.ben-manes.versions' version '0.56.0'
    }
}
rootProject.name = 'tuarkus'
