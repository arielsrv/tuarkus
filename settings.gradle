pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
        id "${benManesPluginId}" version "${benManesPluginVersion}"
    }
}
rootProject.setName('tuarkus')
